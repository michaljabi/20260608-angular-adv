
// Lista ikon osobno, żeby nie robić "natłoku" config'a, i go podzielić "per intent".

import { faSolidCopy, faSolidUser, faSolidUsers } from '@ng-icons/font-awesome/solid';

export const iconSelectionMapper = {
  // Mapuje na uproszczone nazwy, dla wygody użytkowania:
  copy: faSolidCopy,
  user: faSolidUser,
  users: faSolidUsers,
};